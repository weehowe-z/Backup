figure 0-15 是每个子问题的决策面
semi 0-3 是经过min之后的决策面
final 是最后的决策面

训练时间上只用BP网络的速度要快于MinMax,即使在使用了更大的隐层节点数的情况下.
决策面则是MinMax的效果稍微好一些.

分组策略采用的是随机,对训练集处理之后shuffle,保证每个子问题的规模一致.

最大训练代数 10000
学习速率 0.05
隐层节点个数 20
